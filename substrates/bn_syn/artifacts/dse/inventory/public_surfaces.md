# Public Surfaces

- CLI: `bnsyn` via `src/bnsyn/cli.py`.
- Python package: `src/bnsyn/*` modules installed from `pyproject.toml`.
- Governance tooling: `scripts/*` invoked by Makefile and CI workflows.
