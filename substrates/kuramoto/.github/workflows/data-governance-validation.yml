name: Disabled (data-governance-validation)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-data-governance-validation-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "data-governance-validation.yml disabled during CI stabilization."
