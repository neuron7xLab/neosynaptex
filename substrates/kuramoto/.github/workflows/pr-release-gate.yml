name: Disabled (pr-release-gate)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-pr-release-gate-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "pr-release-gate.yml disabled during CI stabilization."
