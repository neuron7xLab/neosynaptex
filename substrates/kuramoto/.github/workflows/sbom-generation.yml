name: Disabled (sbom-generation)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-sbom-generation-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "sbom-generation.yml disabled during CI stabilization."
