name: Disabled (exchange-matrix)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-exchange-matrix-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "exchange-matrix.yml disabled during CI stabilization."
