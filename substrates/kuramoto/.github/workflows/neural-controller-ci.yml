name: Disabled (neural-controller-ci)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-neural-controller-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "neural-controller-ci.yml disabled during CI stabilization."
