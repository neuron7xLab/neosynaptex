name: Disabled (dependency-review)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-dependency-review-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "dependency-review.yml disabled during CI stabilization."
