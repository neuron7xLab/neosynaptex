name: Disabled (ci-hardening)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-ci-hardening-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "ci-hardening.yml disabled during CI stabilization."
