name: Disabled (multi-exchange-replay-regression)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-multi-exchange-replay-regression-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "multi-exchange-replay-regression.yml disabled during CI stabilization."
