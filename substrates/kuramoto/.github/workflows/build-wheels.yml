name: Disabled (build-wheels)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-build-wheels-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "build-wheels.yml disabled during CI stabilization."
