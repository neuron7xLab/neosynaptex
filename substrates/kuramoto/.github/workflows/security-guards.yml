name: Disabled (security-guards)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-security-guards-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "security-guards.yml disabled during CI stabilization."
