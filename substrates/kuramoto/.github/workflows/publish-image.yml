name: Disabled (publish-image)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-publish-image-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "publish-image.yml disabled during CI stabilization."
