name: Disabled (load-test)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-load-test-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "load-test.yml disabled during CI stabilization."
