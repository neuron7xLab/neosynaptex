name: Disabled (mutation-testing)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-mutation-testing-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "mutation-testing.yml disabled during CI stabilization."
