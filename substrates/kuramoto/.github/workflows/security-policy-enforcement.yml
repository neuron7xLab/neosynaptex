name: Disabled (security-policy-enforcement)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-security-policy-enforcement-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "security-policy-enforcement.yml disabled during CI stabilization."
