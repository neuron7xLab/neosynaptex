name: Disabled (sbom)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-sbom-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "sbom.yml disabled during CI stabilization."
