name: Disabled (dopamine-validation)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-dopamine-validation-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "dopamine-validation.yml disabled during CI stabilization."
