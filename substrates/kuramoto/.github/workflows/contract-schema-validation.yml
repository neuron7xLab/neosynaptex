name: Disabled (contract-schema-validation)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-contract-schema-validation-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "contract-schema-validation.yml disabled during CI stabilization."
