name: Disabled (helm)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-helm-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "helm.yml disabled during CI stabilization."
