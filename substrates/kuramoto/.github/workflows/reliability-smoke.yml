name: Disabled (reliability-smoke)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-reliability-smoke-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "reliability-smoke.yml disabled during CI stabilization."
