name: Disabled (ossf-scorecard)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-ossf-scorecard-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "ossf-scorecard.yml disabled during CI stabilization."
