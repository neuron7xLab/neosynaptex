name: Disabled (performance-regression)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-performance-regression-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "performance-regression.yml disabled during CI stabilization."
