name: Disabled (bibliography-lint)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-bibliography-lint-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "bibliography-lint.yml disabled during CI stabilization."
