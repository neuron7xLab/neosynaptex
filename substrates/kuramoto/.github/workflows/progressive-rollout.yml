name: Disabled (progressive-rollout)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-progressive-rollout-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "progressive-rollout.yml disabled during CI stabilization."
