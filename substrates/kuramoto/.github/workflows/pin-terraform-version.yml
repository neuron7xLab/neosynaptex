name: Disabled (pin-terraform-version)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-pin-terraform-version-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "pin-terraform-version.yml disabled during CI stabilization."
