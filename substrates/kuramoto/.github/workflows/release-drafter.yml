name: Disabled (release-drafter)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-release-drafter-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "release-drafter.yml disabled during CI stabilization."
