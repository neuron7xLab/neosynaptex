name: Disabled (enterprise-cicd)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-enterprise-cicd-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "enterprise-cicd.yml disabled during CI stabilization."
