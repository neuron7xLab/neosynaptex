name: Disabled (publish-python)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-publish-python-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "publish-python.yml disabled during CI stabilization."
