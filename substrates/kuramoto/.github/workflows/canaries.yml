name: Disabled (canaries)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-canaries-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "canaries.yml disabled during CI stabilization."
