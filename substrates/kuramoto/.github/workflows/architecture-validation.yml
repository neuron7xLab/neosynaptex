name: Disabled (architecture-validation)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-architecture-validation-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "architecture-validation.yml disabled during CI stabilization."
