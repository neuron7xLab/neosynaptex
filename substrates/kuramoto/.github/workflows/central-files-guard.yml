name: Disabled (central-files-guard)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-central-files-guard-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "central-files-guard.yml disabled during CI stabilization."
