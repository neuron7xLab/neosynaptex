name: Disabled (dependency-pinning)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-dependency-pinning-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "dependency-pinning.yml disabled during CI stabilization."
