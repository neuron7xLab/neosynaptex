name: Disabled (deploy-environments)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-deploy-environments-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "deploy-environments.yml disabled during CI stabilization."
