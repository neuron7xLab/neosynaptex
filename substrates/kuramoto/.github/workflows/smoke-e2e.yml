name: Disabled (smoke-e2e)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-smoke-e2e-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "smoke-e2e.yml disabled during CI stabilization."
