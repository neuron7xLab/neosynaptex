name: Disabled (thermo-evolution)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-thermo-evolution-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "thermo-evolution.yml disabled during CI stabilization."
