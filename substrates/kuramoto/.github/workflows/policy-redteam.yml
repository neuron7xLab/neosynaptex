name: Disabled (policy-redteam)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-policy-redteam-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "policy-redteam.yml disabled during CI stabilization."
