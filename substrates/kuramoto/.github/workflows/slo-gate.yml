name: Disabled (slo-gate)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: disabled-slo-gate-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Disabled workflow
        run: echo "slo-gate.yml disabled during CI stabilization."
