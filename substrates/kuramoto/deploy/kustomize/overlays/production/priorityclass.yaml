apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: tradepulse-critical
value: 1000000000
preemptionPolicy: PreemptLowerPriority
globalDefault: false
description: "Ensures TradePulse production workloads remain schedulable under cluster pressure."
