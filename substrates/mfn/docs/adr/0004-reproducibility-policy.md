# ADR 0004: Reproducibility policy

- Status: Accepted
- Each canonical run emits manifest, config hash, seed, engine version, runtime metadata, and artifact hashes.
- Benchmarks and validation write machine-readable JSON + CSV artifacts.
