# Worked Example — SE Operational Readiness

This worked example demonstrates the full execution path:
1. load the SE protocol and hardening layer;
2. validate canonical artifacts;
3. compute task scores;
4. aggregate domain scores;
5. evaluate gates;
6. emit a conformant `EvaluationResult`.

## Input
- `SE_WORKED_EXAMPLE_INPUT.json`

## Output
- `SE_WORKED_EXAMPLE_OUTPUT.json`

## Why this matters
This repository now includes a concrete, end-to-end trace rather than only abstract protocol text.
